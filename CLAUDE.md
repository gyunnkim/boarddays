# boarddays --- Claude Code 프로젝트 헌법

## 1. 프로젝트 개요

boarddays은 사용자가 자신의 보드게임 전적을 기록하고 관리하는 웹
서비스다.

현재 지원 게임: - 듄: 임페리움 - SETI - 테라포밍 마스

boarddays은 새 프로젝트로 시작한다. 기존 TFMCounter는 필요한 경우 참고
자료로만 사용한다. 기존 프로젝트의 구조, DB, 네이밍, Git 이력에
boarddays을 종속시키지 않는다.

## 2. 기술 스택

-   Next.js App Router
-   TypeScript
-   Tailwind CSS
-   Supabase PostgreSQL
-   Supabase Auth
-   Vercel

현재 저장소에서 선택한 안정 버전을 우선 사용한다. 편의를 위해 주요
의존성을 임의로 업그레이드하지 않는다.

## 3. 핵심 제품 원칙

1.  매치(match)를 핵심 도메인 객체로 취급한다.
2.  새 매치를 만들기 전에 반드시 게임과 확장팩을 선택한다.
3.  선택한 확장팩은 해당 매치에 저장한다.
4.  매치 입력 화면은 저장된 확장팩을 기준으로 동작한다.
5.  확장팩 전용 기능을 UI에 하드코딩하지 않고 데이터/Capability 기반으로
    처리한다.
6.  최초 매치 입력에서 날짜를 직접 입력받지 않는다. 시스템이 현재 날짜를
    기본 저장한다.
7.  과거 전적에는 사용한 확장팩, 내 점수, 순위, 승패가 명확히 표시된다.
8.  모든 사용자 소유 데이터는 인증 사용자별로 격리한다.
9.  DB 제약조건과 RLS를 적극적으로 활용한다.
10. 기능 구현보다 데이터 무결성, 보안, 유지보수성을 우선한다.

## 4. 게임 및 확장팩

### 듄: 임페리움

-   익스의 부상
-   불멸

### SETI

-   우주기관

### 테라포밍 마스

-   헬라스 & 엘리시움
-   비너스 넥스트
-   서곡
-   개척기지
-   격동
-   아마조니스 & 보레알리스
-   유토피아 & 킴메리아
-   업적과 기업상
-   서곡 2

확장팩은 안정적인 slug를 사용한다. 화면 표시명과 내부 식별자를 분리한다.

게임별 세부 문서(slug, 확장팩 목록, 매치 입력 필드 초안, capability 확인
필요 항목)는 다음을 참조한다.

-   `docs/games/dune-imperium.md`
-   `docs/games/seti.md`
-   `docs/games/terraforming-mars.md`

`docs/games/gloomhaven.md`는 향후 지원을 검토하기 위한 beta 참조
문서다. 글룸헤이븐은 위 세 게임과 달리 캠페인/협동/시나리오 기반
구조라 기존 match 모델을 그대로 적용하기 어렵다. 현재 지원 게임
목록(3종)에 포함하지 않으며, 별도 지시 없이 구현하지 않는다.

## 5. 게임 도메인 원칙

게임별 규칙과 확장팩별 기능은 일반 매치 UI와 분리한다.

예: - 테라포밍 마스의 개척기지 확장팩이 선택된 경우에만 개척기지 기능을
활성화한다. - 선택되지 않은 확장팩의 기능은 활성화하지 않는다.

다음과 같은 조건문을 여러 컴포넌트에 반복해서 만들지 않는다.

`game === "terraforming-mars" && expansion === "colonies"`

게임 설정과 Capability를 통해 현재 매치가 어떤 기능을 사용할 수 있는지
판단한다.

## 6. 아키텍처

-   Server Component를 기본값으로 사용한다.
-   Client Component는 상호작용이나 브라우저 API가 필요한 경우에만
    사용한다.
-   도메인 로직과 화면 표시 로직을 분리한다.
-   Supabase 서버/브라우저 클라이언트를 분리한다.
-   Server Action/Route Handler는 인증과 권한을 확인한 후 mutation을
    수행한다.
-   `any`를 사용하지 않는다.
-   실제 반복되는 문제를 해결할 때만 추상화를 추가한다.

## 7. 데이터베이스

핵심 도메인: - profiles - games - expansions - matches - match_players -
match_expansions

Supabase `auth.users`가 인증의 기준이다.

사용자 소유 테이블에는 RLS를 반드시 적용한다.

모든 스키마 변경은 migration으로 관리한다.

## 8. 보안

-   service-role key를 브라우저에 노출하지 않는다.
-   `.env` 및 비밀값을 커밋하지 않는다.
-   `NEXT_PUBLIC_*`에는 브라우저 노출이 안전한 값만 넣는다.
-   클라이언트가 전달한 user_id를 권한 검증에 사용하지 않는다.
-   인증 세션을 서버에서 확인한다.
-   사용자 소유 데이터의 CRUD 정책을 RLS로 보호한다.
-   RLS를 편의상 비활성화하거나 우회하지 않는다.

## 9. UI

깔끔하고 현대적인 다크 모드 중심 UI를 지향한다.

중요: - 명확한 정보 계층 - 반응형 - 접근성 - 확장팩 선택 상태의 명확한
표현 - 읽기 쉬운 전적 - 일관된 게임/확장팩 badge - 로딩/빈 상태/오류
상태

명시적으로 선택하지 않은 대규모 UI 라이브러리를 추가하지 않는다.

## 10. 개발 절차

중요한 작업은 다음 순서로 진행한다.

1.  현재 코드 확인
2.  관련 문서/rules 확인
3.  영향 범위 파악
4.  구현 계획
5.  최소 범위 구현
6.  테스트
7.  typecheck/lint/build
8.  최종 diff 검토

요청과 관계없는 리팩터링을 하지 않는다.

## 11. TFMCounter 참고 원칙

TFMCounter에서 좋은 구현을 발견하면 선택적으로 참고할 수 있다.

하지만: - 기존 구조를 무조건 복사하지 않는다. - 기존 DB를 그대로
사용하지 않는다. - 기존 네이밍을 강제하지 않는다. - boarddays의
요구사항을 항상 우선한다.

## 12. 완료 기준

작업 완료 전 확인: - TypeScript 오류 없음 - lint 통과 - 관련 테스트
통과 - 필요한 경우 production build 통과 - DB 변경 시 RLS 검토 -
인증/권한 검토 - UI 상태 처리 - 범위 밖 변경 없음

실제로 실행하지 않은 검사를 통과했다고 보고하지 않는다.

## 13. Next.js 버전 주의사항

@AGENTS.md
